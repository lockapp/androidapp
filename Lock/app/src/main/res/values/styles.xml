<resources>

    <style name="TextShadow">
        <item name="android:shadowColor">#000000</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">1</item>
    </style>


    <style name="Spinner">
        <item name="android:indeterminate">true</item>
        <item name="android:indeterminateDrawable">@drawable/spinner</item>
        <item name="android:indeterminateDuration">2500</item>
        <item name="android:indeterminateOnly">true</item>
    </style>

    <style name="LockProgressListSpinner" parent="Spinner">
        <item name="android:layout_width">64dp</item>
        <item name="android:layout_height">64dp</item>
    </style>

    <style name="SpinnerCircular">
        <item name="android:indeterminate">true</item>
        <item name="android:indeterminateDrawable">@drawable/spinnercircular</item>
        <item name="android:indeterminateDuration">2500</item>
        <item name="android:indeterminateOnly">true</item>
    </style>


    <style name="circularProgressListSpinner" parent="SpinnerCircular">
        <item name="android:layout_width">64dp</item>
        <item name="android:layout_height">64dp</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.AppTheme" />

    <style name="AppTheme.OverlapSystemBar" parent="Base.AppTheme" />

    <style name="Base.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:popupBackground">#0F213F</item>
        <item name="android:disabledAlpha">0.5</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- android:textColorPrimary is the  color of the title text
       in the Toolbar, in the Theme.AppCompat theme:  -->
        <!--<item name="android:textColorPrimary">@color/abc_primary_text_material_light</item>-->

        <!-- android:textColorPrimaryInverse is the  color of the title
             text in the Toolbar, in the Theme.AppCompat.Light theme:  -->
        <!-- <item name="android:textColorPrimaryInverse">@color/abc_primary_text_material_light</item> -->

        <!-- android:actionMenuTextColor is the color of the text of
              action (menu) items in the Toolbar, at least in the
              Theme.AppCompat theme.
              For some reason, they already get the textColorPrimary
              when running on API 21, but not on older versions of
              Android, so this is only necessary to support older
              Android versions.-->
        <!--<item name="actionMenuTextColor">@color/abc_primary_text_material_light</item>-->
        <!-- android:textColorSecondary is the color of the menu
             overflow icon (three vertical dots) -->
        <item name="android:textColorSecondary">@color/abc_secondary_text_material_dark</item>

        <!-- This would set the toolbar's background color,
              but setting this also changes the popup menu's background,
              even if we define popupTheme for our <Toolbar> -->
        <!-- <item name="android:background">@color/color_primary</item> -->
    </style>


    <!-- tema del dialogo del boton de mas-->
    <style name="MaterialTapTargetPromptTheme"></style>
    <!-- This style is an example of what can be changed and not necessarily what should be changed! -->
    <style name="MaterialTapTargetPromptTheme.FabTarget" parent="MaterialTapTargetPromptTheme">
        <item name="backgroundColour">@color/colorPrimary</item>
        <!--<item name="focalColour">@color/colorPrimary</item>-->
        <!--<item name="primaryText">@string/FabTarget.primaryText</item>
        <item name="secondaryText">@string/FabTarget.secondaryText</item>
        <item name="primaryTextColour">@color/green_200</item>
        <item name="secondaryTextColour">@color/orange_200</item>
            <item name="target">@id/fab</item>
        <item name="focalRadius">88dp</item>
        <item name="primaryTextSize">10sp</item>
        <item name="secondaryTextSize">22sp</item>
        <item name="maxTextWidth">600dp</item>
        <item name="textPadding">80dp</item>
        <item name="focalToTextPadding">8dp</item>
        <item name="textSeparation">0dp</item>
        <item name="primaryTextTypeface">serif</item>
        <item name="secondaryTextTypeface">monospace</item>
        <item name="secondaryTextStyle">italic</item>
        <item name="iconColourFilter">@color/green_200</item>-->
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

</resources>
